const mongoose = require("mongoose");

// const senderPayment = new mongoose.Schema({

    
// });

// const SenderPayment = mongoose.model("senderpayment", senderPayment);

// module.exports = SenderPayment;
